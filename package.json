{
  "name": "neurolearn-assist-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "caddy run --config Caddyfile --adapter caddyfile"
  }
}
